# Project Identity
PROJECT_NAME=sinhala-tts
LANG_NAME=Sinhala
LANG_LOCALE=si_LK
VOICE_NAME=ashoka
INPUT_DATA_DIR=SinhalaTTSData

# S3 Configuration
S3_BUCKET=sinhala-tts-piper
S3_REGION=us-west-2
S3_DATA_PREFIX=training-data
S3_CHECKPOINT_PREFIX=checkpoints

# EC2 Configuration
EC2_INSTANCE_TYPE=g6.xlarge
EC2_REGION=us-west-2
EC2_AMI=ami-0c02fb55956c7d316  # Amazon Linux 2 (will be overridden by setup)
EC2_KEY_NAME=sinhala-tts-key
EC2_SPOT_MAX_PRICE=0.60
EC2_USE_SPOT=true

# Training Configuration
PIPER_LANGUAGE=si
PIPER_SAMPLE_RATE=22050
PIPER_BATCH_SIZE=32
PIPER_MAX_EPOCHS=6000
PIPER_CHECKPOINT_EPOCHS=25
PIPER_PRECISION=32
PIPER_QUALITY=medium

# Base checkpoint for fine-tuning
BASE_CHECKPOINT_REPO=rhasspy/piper-checkpoints
BASE_CHECKPOINT_PATH=hi/hi_IN/rohan/medium
BASE_CHECKPOINT_LANG=hi_IN
BASE_CHECKPOINT_NAME=Hindi

# Local paths (on EC2)
EC2_WORK_DIR=/home/ubuntu/training
EC2_DATA_DIR=/home/ubuntu/training/data
EC2_OUTPUT_DIR=/home/ubuntu/training/output
EC2_CHECKPOINT_DIR=/home/ubuntu/training/checkpoints

# Sample texts for the Hugging Face model card (one per line).
# These are synthesized into audio during publishing.
SAMPLES="
ආයුබෝවන්.
සිංහල භාෂාවෙන් කතා කිරීමට මෙම මාදිලිය භාවිතා කළ හැකිය.
මේ ලස්සන දිනයක් වගේ පෙනෙනවා.
අද කාලගුණය ඉතා සුන්දරයි.
"

# Derived (do not edit)
ONNX_NAME=${LANG_LOCALE}-${VOICE_NAME}-${PIPER_QUALITY}
